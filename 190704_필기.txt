패키지명은소문자
클래스는 앞 대문자 단어끝나면 다음문자 앞 대문자
heap영역에 배열저장

data1, data2 는 별도의 주소값을 가짐

배열의 안에는 data1과 data2 주소값을 참조

메인메소드 args는 자동으로 stack 영역에생성

stack안에서는 메소드가종료되면 자동으로 휘발

문자열이기때문에 heap영역에 생성

객체생성은 멤버변수로만 생성
heap영역에는 멤버변수
stack영역에는 지역변수

기본타입이 아닌 모든것들은 무조건 다 객체


	public static boolean delete(String name){
		int arrayIndex = 0;
		for(People v : allData) {
			if(v.getName().equals(name)) {//
				allData[arrayIndex] = null;
				
				return true;
			}
			arrayIndex++;
		}
		return false;
	}
	
	
new를 쓰지 않고 객체를 생성하는것은 문자열과 배열 (기본타입 기반의 배열 포함) - heap영역에 저장

	People[] allData = PeopleModel.getAll();
			EndView.printAll(allData)